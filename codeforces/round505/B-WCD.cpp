#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int mian(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int n = 0;
    cin >> n;
    ll a1,b1;
    cin >> a1 >> b;
}
